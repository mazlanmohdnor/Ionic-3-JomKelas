export interface Profile {
    fullname: string,
    photoURL: string,
    password:string,
    email: string,
    phoneNumber: string,
    matric: number;
    bio: string,
    kolej: string,
    gender:string,
    rate:number,
    profileComplete:boolean,
    vehicleComplete:boolean
}